export const emptyStub = {};

export const createTtsService = () => {
  throw new Error("TTS engine unavailable in browser client bundle");
};

export const createAudioPlayer = () => {
  throw new Error("TTS engine unavailable in browser client bundle");
};

export const createTtsOrchestrator = () => {
  throw new Error("TTS engine unavailable in browser client bundle");
};