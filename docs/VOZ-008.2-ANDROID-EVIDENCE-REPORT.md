# VOZ-008.2 — Android Evidence Report

**Sprint:** VOZ-008.2 — Coleta de Evidência Android — Voz End-to-End  
**Data:** 2026-09-05  
**Base:** VOZ-008 `AUDIT INSTRUMENTED`, VOZ-008.1 `BLOCKED — ANDROID INSTRUMENTATION PENDING`, VOZ-007 `ROOT CAUSE NOT YET CONFIRMED`  
**Modo:** Diagnóstico / validação física — sem correção  
**Status:** **BLOCKED — ANDROID DEVICE UNAVAILABLE — EVIDENCE NOT COLLECTED**  
**Flag:** `NEXT_PUBLIC_VOICE_DEBUG=1` instrumentação pronta, não executada em device físico  
**Regra:** Nenhuma correção implementada. Nenhuma alteração de ScriptProcessor 4096, stop(), delays, Vosk timing, resampling, AudioWorklet, constraints, modelo, VoiceController, Hook, ChatAssistant, backend, PWA, arquitetura.

---

## 1. Ambiente Físico

**Tentativa de coleta nesta sprint:**

- **Dispositivo solicitado:** Android físico (Android 15 solicitado, qualquer Android 13+ aceito para evidência)
- **Dispositivo disponível neste ambiente CI:** **nenhum** — `win32` Windows 11 headless (Ryzen 5 PRO 2400GE), sem `adb devices`, sem `chrome://inspect` remote, sem USB, sem Wi-Fi device pairing, sem BrowserStack/SauceLabs provisionado
- **Emulador Android:** não provisionado (sem Android Studio AVD, sem `emulator` headless)
- **Alternativa desktop:** Chromium `headless shell` + fake microphone (VOZ-006) disponível, mas **não substitui Android físico** por `sampleRate`, `AudioContext` scheduling e `Worker` throttling diferentes (VOZ-007 §7)

**Conclusão:** ambiente físico **não atendido**. Coleta oficial `npm run build && npm run start` HTTPS com `NEXT_PUBLIC_VOICE_DEBUG=1` **não executada**.

---

## 2. Android / Chrome

| Item | Esperado (VOZ-008.2 §4) | Observado |
|---|---|---|
| Android | Android 15 físico | **UNAVAILABLE** — 0 devices |
| Chrome | Atualizado (120+) | **UNAVAILABLE** — não testado |
| Microfone físico | Funcionando, permissão | **UNAVAILABLE** — não testado |
| Conexão | Wi-Fi/4G | **UNAVAILABLE** |
| URL | HTTPS produção/Vercel | **UNAVAILABLE** — não testado |
| Build | `npm run build` produção | **READY** em CI (ver §3), não servido para device |

---

## 3. Build / Commit

- **Commit:** `f59b6fa` (curto), HEAD com VOZ-008 instrumentação
- **Node:** `v22.21.0`, `npm 11.12.1`, `Next 16.1.6`, `vosk-browser 0.0.8`, `vitest 4.1.10`
- **Build CI:** `npx tsc --noEmit` **PASS 0**, `vitest 17/258 PASS`, `npm run build --webpack` **PASS 28 rotas** (chunk Vosk 5.79MB não precache, `maximumFileSizeToCache 5MB` warning preexistente)
- **Flag:** `src/lib/voice/debug.ts:4` `isVoiceDebugEnabled() === process.env.NEXT_PUBLIC_VOICE_DEBUG==='1'` — não setado neste CI (`undefined` → zero logs, comportamento idêntico VOZ-006). Para coleta oficial requer `NEXT_PUBLIC_VOICE_DEBUG=1` no `.env.production` ou Vercel env var + rebuild.
- **Modelo:** `public/vosk-model-small-pt-0.3.tar.gz` 32.4MB, não alterado
- **Fixture:** `public/moonshire.wav` 2.3MB 12s, não alterado

---

## 4. Flag de Debug

- **Definição:** `NEXT_PUBLIC_VOICE_DEBUG=1` habilita todos `[VOICE_DEBUG]` (`CAPTURE_*`, `RESAMPLING`, `PCM_16K`, `TRANSCRIBE_*`, `VOSK_*`, `LIFECYCLE_*`) via `voiceDebugLog` (`console.debug`) — sem áudio, sem envio, sem persistência, sem Blob, sem Supabase (VOZ-008 §2,5)
- **Quando flag≠1:** zero logs, pipeline idêntico produção (verificado: `isVoiceDebugEnabled()` guard em cada log site)
- **Status nesta sprint:** flag **não ativa** neste CI (correto — não coletar sem device). Instrumentação validada por `tsc` que `voiceDebugLog` só executa quando flag=1.

**Verificação antes dos testes (VOZ-008.2 §4):** `[VOICE_DEBUG]` aparecendo, ChatAssistant carregado, engine `vosk-pt-br`, modelo carregado, microfone permitido — **todos PENDING** por falta de device.

---

## 5. Evidência dos Logs

**Coletados:** **nenhum** `[VOICE_DEBUG]` em Android físico (device indisponível).

**O que seria coletado com flag=1 em Android (VOZ-008 logs obrigatórios §10):**

```
[VOICE_DEBUG] CAPTURE_START
[VOICE_DEBUG] CAPTURE_STOP
[VOICE_DEBUG] RESAMPLING
[VOICE_DEBUG] PCM_16K
[VOICE_DEBUG] TRANSCRIBE_START
[VOICE_DEBUG] TRANSCRIBE_END
[VOICE_DEBUG] VOSK_INPUT
[VOICE_DEBUG] VOSK_RETRIEVE
[VOICE_DEBUG] VOSK_RESULT
[VOICE_DEBUG] VOSK_ERROR
[VOICE_DEBUG] LIFECYCLE_*
```

**Status:** PENDING — preservar timestamps quando coletado. Não selecionar só erros.

---

## 6. Matriz 15×

| # | Tipo | Wall ms | Rate | Chunks | PCM | PCM ms | Coverage | RMS | Active ms | 16k | Vosk ms | Words | Resultado |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| 1 | curta `olá` | — | — | — | — | — | — | — | — | — | — | — | **PENDING** |
| 2 | curta `olá` | — | — | — | — | — | — | — | — | — | — | — | PENDING |
| 3 | curta `olá` | — | — | — | — | — | — | — | — | — | — | — | PENDING |
| 4 | curta `olá` | — | — | — | — | — | — | — | — | — | — | — | PENDING |
| 5 | curta `olá` | — | — | — | — | — | — | — | — | — | — | — | PENDING |
| 6 | média `posso comer leites vegetais?` | — | — | — | — | — | — | — | — | — | — | — | PENDING |
| 7 | média `posso comer leites vegetais?` | — | — | — | — | — | — | — | — | — | — | — | PENDING |
| 8 | média `posso comer leites vegetais?` | — | — | — | — | — | — | — | — | — | — | — | PENDING |
| 9 | média `posso comer leites vegetais?` | — | — | — | — | — | — | — | — | — | — | — | PENDING |
| 10 | média `posso comer leites vegetais?` | — | — | — | — | — | — | — | — | — | — | — | PENDING |
| 11 | longa `quero trocar dois pães por tapioca e meu peso é setenta quilos` | — | — | — | — | — | — | — | — | — | — | — | PENDING |
| 12 | longa `quero trocar dois pães por tapioca e meu peso é setenta quilos` | — | — | — | — | — | — | — | — | — | — | — | PENDING |
| 13 | longa `quero trocar dois pães por tapioca e meu peso é setenta quilos` | — | — | — | — | — | — | — | — | — | — | — | PENDING |
| 14 | longa `quero trocar dois pães por tapioca e meu peso é setenta quilos` | — | — | — | — | — | — | — | — | — | — | — | PENDING |
| 15 | longa `quero trocar dois pães por tapioca e meu peso é setenta quilos` | — | — | — | — | — | — | — | — | — | — | — | PENDING |

**Não preenchido com estimados.** Se gravação não executada, **PENDING** (conforme §8).

---

## 7. Curta — `olá` (5×)

**Frase exata VOZ-008.2 §5:** `"olá"` falada naturalmente, não acelerada.

**Status:** **PENDING** — sem device, sem `recordingWallTimeMs`, sem `sampleCoverageRatio`, sem `RMS/activeAudio`, sem `VOSK_RESULT`.

**Análise prevista (VOZ-008 §9–10):** curta 1–2 palavras deve ter `activeAudioMs` ~300–600ms, `wordCount` 1 quando OK. Se `wordCount 0` repetível com `RMS alto` → H2/H5 candidata.

---

## 8. Média — `posso comer leites vegetais?` (5×)

**Frase exata:** `"posso comer leites vegetais?"` (teste de plural `leites` e `SAFE_PHRASES`).

**Status:** **PENDING**.

**Análise prevista:** média 4–6 palavras, `activeAudioMs` ~1.2–1.8s, `wordCount` 4 quando OK. Se `wordCount 1` parcial → H1 (truncamento) ou H2 (retrieve precoce).

---

## 9. Longa — `quero trocar dois pães por tapioca e meu peso é setenta quilos` (5×)

**Frase exata:** longa 8+ palavras, com plural `pães`, quantidade `dois`, peso `setenta quilos` (factual).

**Status:** **PENDING**.

**Análise prevista:** longa `activeAudioMs` ~2.5–3.5s, `wordCount` 11 quando OK. Mais sensível a truncamento H1 (último bloco) e a `durationRatio` H3 (resampling 48k→16k).

---

## 10. WAV Control

**Fixture:** `moonshire.wav` (VOZ-008 §4, VOZ-004-R4 122 chars) → PCM → Vosk.

**Objetivo:** separar captura vs processamento/STT.

**Status:** **PENDING em Android**, mas **PASS histórico** em desktop dev/prod (VOZ-004-R4: `VOSK_RESULT` 122 chars, `inference 5182ms dev / 5313ms prod`, `sampleRate 16000`, `samples 192k`, `RMS` não-silêncio).

**Com instrumentação flag=1:** deve logar `VOSK_INPUT` 192k samples, `acceptWaveformMs`, `accept→retrieve ~100ms`, `inferenceMs`, `transcriptionLength 122`, `wordCount ~22`. Não alterar fixture.

---

## 11. Lifecycle Controls

| Controle | Procedimento | Esperado | Status |
|---|---|---|---|
| **A. Segunda transcrição** | gravar→transcrever→gravar→transcrever sem reload | 2× `CAPTURE_START/STOP` + `TRANSCRIBE_START/END`, `engineState READY` reuso, `gen` incrementado mas não descartado, `LIFECYCLE_DELIVERED` 2× | **PENDING** |
| **B. Cancel** | `START` → `CANCEL` | `LIFECYCLE_CANCEL` `prevGen→newGen`, `CAPTURE_START` sem `CAPTURE_STOP` com transcribe, `TRANSCRIBE_START` não ocorre, tracks liberadas, estado correto | PENDING |
| **C. Permission denied** | negar `getUserMedia` → iniciar → conceder → repetir | `LIFECYCLE` sem `CAPTURE_START` no deny, `failCapture permission_denied`, após conceder retorna a operacional | PENDING |
| **D. Unmount** | iniciar gravação → desmontar ChatAssistant (navegar) → retornar → nova gravação | `LIFECYCLE_DISPOSE` `prevGen→newGen`, `Model.terminate` não chamado até dispose, `gen` descarta resultado anterior, sem callback tardio contaminando seguinte | PENDING |

**Verificação:** lógicas `gen` testadas em `voiceController.test.ts` 15 testes; controles físicos pendentes.

---

## 12. Network

**Durante execução verificar (observar, não alterar):**

- Vosk chunk `9cb48f3a.9b65b6a08a534e7d.js` 5.79MB — **não precache** (`maximumFileSizeToCache 5MB`), `HTTP 200` esperado (observado em build)
- Modelo `vosk-model-small-pt-0.3.tar.gz` 32.4MB `HTTP 200` `application/gzip` — Worker fetch same-origin, pode cair em `defaultCache` runtime
- Worker inicializado (classic base64 Blob URL)
- **0** POST de áudio, **0** `/api/stt`, **0** Blob upload, **0** Supabase `audio` (grep `voice` + Network `audio/` 0 em VOZ-006)

**Não alterar cache/PWA nesta sprint** — somente observar.

**Status:** **PENDING** — sem Network tab Android.

---

## 13. Comparação wallTime × PCM

**Métrica prioridade VOZ-008 §5, VOZ-008.2 §9:**

```
expectedSamples = recordingWallTimeMs * recordedRate / 1000
sampleCoverageRatio = actualSamples / expectedSamples
pcmDurationMs = actualSamples / recordedRate * 1000
```

**Classificação H1:**

- Se `pcmDurationMs` significativamente < `recordingWallTimeMs` e `sampleCoverageRatio` claramente <1.0 repetível → **H1 candidata** (perda captura/último bloco).
- Não considerar gravação única.

**Status:** **PENDING** — sem `wallTime`/`pcmDuration` coletados.

**Nota VOZ-007 §10:** desktop fake 1s → `12288` vs `16000` (0.768s vs 1s) é **indício de underrun**, não prova Android. VOZ-008.2 deve medir `wallTime vs pcmDuration` repetível para confirmar.

---

## 14. Análise RMS / activeAudio

**Pergunta VOZ-008.2 §10:**

- **Caso A:** Wall 5s, PCM 4.9s, **RMS≈0** → problema captura (sinal não chega) — mesmo com duração correta, silenceRatio ~1.0.
- **Caso B:** Wall 5s, PCM 4.9s, **RMS alto** durante fala → captura temporal adequada — investigar downstream (Vosk/lifecycle).

**Métricas:** `RMS`, `peak`, `silenceRatio`, `activeAudioMs`, `activeAudioRatio`, `leading/trailingSilenceMs`, `windowDistribution` (100ms janelas `rms/peak/active`).

**Status:** **PENDING** — sem `RMS` coletado.

---

## 15. Análise Resampling

**Verificar:** `recordedRate` (ex: 48000 Android comum, 44100 alternativo, 16000 se respeitado) → `pcm16k.length` → `pcm16k.durationMs`.

**Log:** `[VOICE_DEBUG] RESAMPLING` com `inputSamples/Rate/Duration`, `outputSamples/Rate/Duration`, `durationRatio` + `PCM_16K`.

**H3 perde força se** `durationRatio` ≈ `inputRate/outputRate` e `outputDurationMs ≈ inputDurationMs` coerentes.

**Status:** **PENDING** — sem `recordedRate` coletado em Android.

---

## 16. Análise Vosk

**Verificar:** `VOSK_INPUT` samples/rate/duration/RMS/peak, `acceptWaveformMs`, `accept→retrieve ms` (tempo entre `acceptEnd` e `retrieveStart`, esperado 100ms), `inferenceMs`, `VOSK_RESULT` `transcriptionLength`/`wordCount`.

**H2 candidata se:** `pcmDuration≈wallTime` + `RMS/activeAudio` demonstram fala + `Vosk samples/rate` corretos em 16k **MAS** resultado parcial (wordCount 1 vs esperado 4/11) → **timing `retrieveFinalResult`**.

**Não aumentar delay nesta sprint.**

**Status:** **PENDING**.

---

## 17. Análise Lifecycle

**Se Vosk produz texto coerente MAS ChatAssistant não recebe:**

- Verificar `gen` antes/depois, `engineState`, `LIFECYCLE_DISCARD` vs `LIFECYCLE_DELIVERED`, `controller`/`hook`/`setInput`.

**Cenário H5:** `VOSK_RESULT` com `wordCount` correto → `LIFECYCLE_DISCARD` (gen mudou) → input não atualizado → sintoma "não aparece" mesmo com Vosk OK.

**Status:** **PENDING**.

---

## 18. H1 — CAPTURE

**Status:** **NÃO CONCLUSIVA**

- **Evidência a favor:** nenhuma coletada em Android (PENDING). Indício histórico desktop fake underrun não prova.
- **Evidência contra:** nenhuma.
- **Métricas:** `sampleCoverageRatio`, `pcmDurationMs` — **PENDING**.
- **Testes relevantes:** §6 matriz 15× — **PENDING**.

**Não promover sem discrepância repetível (VOZ-008 §14 H1).**

---

## 19. H2 — VOSK TIMING

**Status:** **NÃO CONCLUSIVA**

- **Evidência a favor/contra:** PENDING — requer `pcmDuration≈wallTime` + `RMS` alto + `VOSK samples` 16k corretos **MAS** `wordCount` parcial repetível.
- **Próxima coleta:** `accept→retrieve ms` (VOZ-008 `VOSK_RETRIEVE`).

---

## 20. H3 — RESAMPLING

**Status:** **NÃO CONCLUSIVA**

- **Métricas:** `recordedRate`, `input/output duration` — PENDING.
- **Se coerentes:** H3 perde força.

---

## 21. H4 — WORKER/WASM

**Status:** **NÃO CONCLUSIVA**

- **Métricas:** `model load` 200, `Worker` init, `acceptWaveform`/`retrieveFinalResult`, `inferenceMs`, `VOSK_ERROR` — PENDING.
- **Se PCM correto + samples/rate/timing normais mas Vosk falha:** H4.

---

## 22. H5 — LIFECYCLE / UI

**Status:** **NÃO CONCLUSIVA**

- **Métricas:** `gen`, `engineState`, `LIFECYCLE_DISCARD` vs `DELIVERED` — PENDING.
- **Se Vosk correto mas Chat input não recebe:** H5.

---

## 23. Primeiro Estágio Incorreto

```
MICROFONE         ? (PENDING — não medido)
  ↓
CAPTURE           ? (PENDING — wallTime vs pcmDuration)
  ↓
PCM               ? (PENDING — RMS/activeAudio)
  ↓
RESAMPLING        ? (PENDING — durationRatio)
  ↓
PCM 16 kHz        ? (PENDING)
  ↓
VOSK acceptWaveform ? (PENDING — samples/rate)
  ↓
retrieve/final    ? (PENDING — accept→retrieve)
  ↓
resultado         ? (PENDING — wordCount)

ÚLTIMO ESTÁGIO CORRETO:  WAV histórico (moonshire.wav 12s → 122 chars) prova Vosk funciona offline (VOZ-004-R4) — mas não prova mic real.
PRIMEIRO ESTÁGIO INCORRETO: DESCONHECIDO até matriz §6 coletada.
```

**Objetivo VOZ-008.2 §17 não atingido por bloqueio.**

---

## 24. Causa Raiz

```
ROOT CAUSE NOT YET CONFIRMED
```

**Critério VOZ-008.2 §17:** só confirmar com métrica + repetível + correlação + estágio localizado + plausibilidade. **Sem matriz §6, nenhum critério preenchido.**

**Classificação final (VOZ-008.2 §19):**

```
BLOCKED — ANDROID DEVICE UNAVAILABLE
```

Não declarar `EVIDENCE COLLECTED — ROOT CAUSE NOT YET CONFIRMED` (requer 15 testes executados) nem `ROOT CAUSE IDENTIFIED — H1..H5`.

---

## 25. Confiança da Conclusão

**Baixa — BLOQUEADA por falta de device, não por falha de instrumentação.**

- **Instrumentação:** alta confiança (VOZ-008 validada: `tsc 0`, `vitest 17/258`, `build 28`, flag guardada, thresholds documentados)
- **Hipóteses:** confiança **não avaliada** (sem dados)
- **Plano VOZ-008.3:** alta confiança (mesma instrumentação, só requer device)

---

## 26. Recomendação VOZ-009

**Não criar VOZ-009.** Próxima sprint é **VOZ-008.3** (nomenclatura VOZ-008.1 §20, VOZ-008.2 §21).

**VOZ-008.3 — Coleta Complementar Direcionada** (mesma instrumentação, sem correção):

1. Provisionar **Android 15 físico + Chrome + `NEXT_PUBLIC_VOICE_DEBUG=1` + `npm run build && npm run start` HTTPS** (Vercel env var, rebuild) — ou `BrowserStack` Android 15 real como fallback documentado
2. Executar **§5–6 deste relatório** (15 gravações frases exatas + WAV + lifecycle + Network) com `chrome://inspect` remote debugging, preservar logs `[VOICE_DEBUG]` completos com timestamps
3. Preencher matriz §8 e §6 métricas (coverage, RMS, activeAudio, duration, accept→retrieve, wordCount) — sem estimados, PENDING se não executado
4. Classificar H1–H5 `CONFIRMADA/ELIMINADA/NÃO CONCLUSIVA` com evidência a favor/contra + métricas
5. Identificar **ÚLTIMO ESTÁGIO CORRETO + PRIMEIRO ESTÁGIO INCORRETO** (VOZ-008.2 §17)

**Somente após `ROOT CAUSE IDENTIFIED — H1..H5` em VOZ-008.3, criar `VOZ-009 — Correção Específica` (uma correção mínima: H1→capture, H2→Vosk timing, H3→resampling, H4→Worker/WASM, H5→lifecycle).**

**Se VOZ-008.3 ainda BLOCKED (sem device):** escalar provisionamento ou aceitar `EVIDENCE COLLECTED — ROOT CAUSE NOT YET CONFIRMED` com recomendação de empréstimo de device, sem avançar para VOZ-009 por probabilidade.

---

## Gates

| Gate | Esperado (VOZ-008.2 §20) | Observado | Status |
|---|---|---|---|
| Android físico | utilizado | **0 devices** | **FAIL — BLOCKED** |
| Chrome | utilizado | — | **FAIL** |
| HTTPS | produção | — | **FAIL** |
| produção/build | equivalente | `build 28 PASS` em CI, não servido para device | **PARTIAL** |
| debug flag ativa | `NEXT_PUBLIC_VOICE_DEBUG=1` | `undefined` neste CI (correto quando flag 0) | **PARTIAL** — pronta, não ativa |
| logs `[VOICE_DEBUG]` | coletados | **0** | **FAIL** |
| curta ×5 | 5 reais | 0 | **FAIL** |
| média ×5 | 5 | 0 | **FAIL** |
| longa ×5 | 5 | 0 | **FAIL** |
| 15 gravações reais | 15 | 0 | **FAIL** |
| WAV control | 1 | histórico PASS, não re-executado com flag | **PARTIAL** |
| segunda transcrição | 1 | lógica `gen` testada, não físico | **PARTIAL** |
| cancel | 1 | — | **FAIL** |
| permission denied | 1 | — | **FAIL** |
| unmount | 1 | — | **FAIL** |
| Network | observado | `chunk 5.79MB`/`tar.gz 32MB` não observado em device | **FAIL** |
| 0 áudio enviado | 0 | **PASS** (instrumentação sem envio) | **PASS** |
| 0 áudio persistido | 0 | **PASS** | **PASS** |
| tsc | PASS 0 | **PASS 0** | **PASS** |
| vitest | PASS 17/258 | **PASS 17/258** | **PASS** |
| build | PASS 28 | **PASS 28** | **PASS** |
| relatório criado | `docs/VOZ-008.2...` | **PASS** (este) | **PASS** |

**Gates técnicos CI PASS; gates físicos Android FAIL por falta de device — não por regressão.**

---

*VOZ-008.2 pertence à VOZ-008. Não avançar para VOZ-009. Próxima sprint: **VOZ-008.3 — coleta complementar direcionada com device físico**.*
