# VOZ-008.3 — Android Physical Evidence Run

**Sprint:** VOZ-008.3 — Android Physical Evidence Run  
**Data:** 2026-09-05  
**Base:** VOZ-008 `AUDIT INSTRUMENTED`, VOZ-008.1 `BLOCKED — ANDROID INSTRUMENTATION PENDING`, VOZ-008.2 `BLOCKED — ANDROID DEVICE UNAVAILABLE`, VOZ-007 `ROOT CAUSE NOT YET CONFIRMED`  
**Tipo:** Execução / Diagnóstico — sem desenvolvimento do pipeline  
**Status:** **BLOCKED — ANDROID DEVICE UNAVAILABLE — ROOT CAUSE NOT YET CONFIRMED**  
**Flag:** `NEXT_PUBLIC_VOICE_DEBUG=1` instrumentação existente, não executada em device físico  
**Regra:** Nenhuma alteração funcional em `capture.ts`, `PcmRecorder`, `ScriptProcessor 4096`, `stop()`, delays, Vosk timing, `acceptWaveformFloat`/`retrieveFinalResult`, `resampleTo16k`, constraints, `VoiceController`, `useVoiceInput`, `registry`, `engineState`, modelo Vosk, PWA, backend, arquitetura. Sem novos delays, sem AudioWorklet, sem resampler, sem modelo.

---

## 1. Ambiente

**Tentativa de execução nesta sprint:**

- **Dispositivo solicitado:** Android físico (Android 15 solicitado, 13+ aceito)
- **Dispositivo disponível:** **0** — verificado `adb devices` → `adb: command not found` (sem Android SDK Platform-Tools instalado); `emulator.exe` → `False` (sem Android Studio AVD); `chrome://inspect` requer device pareado via USB/Wi-Fi — nenhum device detectado
- **Método de inspeção tentado:** `adb devices`, `Get-Process chrome`, verificação `emulator.exe`, `chrome://inspect` (requer device físico)
- **Motivo do bloqueio:** ambiente CI `win32` Windows 11 headless (Ryzen 5 PRO 2400GE, sem USB, sem Wi-Fi pairing, sem BrowserStack/SauceLabs), sem provisionamento de device farm
- **Alternativa proibida:** não usar fake microphone como substituto (VOZ-008.3 §3), não simular Android, não transformar CI em evidência Android

**Conclusão:** pré-condição obrigatória §2 VOZ-008.3 **não atendida**. Coleta oficial `npm run build && npm run start` HTTPS com `NEXT_PUBLIC_VOICE_DEBUG=1` **não executada** — falharia em `isSecureContext`/`getUserMedia` sem device e não produziria logs reais.

---

## 2. Android

| Item | Esperado (VOZ-008.3 §4) | Observado |
|---|---|---|
| Android | Físico, versão reportada | **UNAVAILABLE** — 0 devices detectados |
| Fabricante/modelo | ex: Pixel 7 / Samsung A54 | **UNAVAILABLE** |

---

## 3. Chrome

| Item | Esperado | Observado |
|---|---|---|
| Chrome | Atualizado (120+) | **UNAVAILABLE** — `chrome` processo existe em host Windows, mas sem Android Chrome remote; `chrome://inspect` sem targets |

---

## 4. Build / Commit

- **Commit:** `f59b6fa` + VOZ-008 instrumentação (4 arquivos: `debug.ts` novo, `capture.ts`, `voiceController.ts`, `vosk.ts` com `isVoiceDebugEnabled` guards)
- **Build:** `npx tsc --noEmit` **PASS 0** (ver §21), `vitest 17/258 PASS`, `npm run build --webpack` **PASS 28 rotas** (VOZ-008.2)
- **Flag:** `NEXT_PUBLIC_VOICE_DEBUG=1` requer rebuild com env var (`NEXT_PUBLIC_` prefix) — não setado neste CI (`undefined` → zero `[VOICE_DEBUG]` logs, comportamento idêntico VOZ-006, correto quando flag 0)
- **Instrumentação:** `src/lib/voice/debug.ts:4` `isVoiceDebugEnabled()`, `capture.ts:191` `RESAMPLING`, `voiceController.ts` `CAPTURE_START/STOP`, `PCM_16K`, `TRANSCRIBE_*`, `LIFECYCLE_*`, `vosk.ts` `VOSK_INPUT/RETRIEVE/RESULT` — todas guardadas

**Status:** build/commit prontos, mas não servidos para device (sem `npm run start` HTTPS com device).

---

## 5. Instrumentação

**Existente e não alterada nesta sprint** (VOZ-008 §3):

- `debug.ts`: `computePcmStats` (samples, durationMs, RMS 4dec, peak, min, max, silenceRatio threshold `0.01`, activeAudioMs/ratio, leading/trailingSilenceMs), `computeWindowDistribution` (janelas 100ms `rms/peak/active`)
- `capture.ts`: `resampleTo16k` loga `inputSamples/Rate/Duration`, `outputSamples/Rate/Duration`, `durationRatio`, `bypassed`
- `voiceController.ts`: `recordingStartMs`, `CAPTURE_START` (engineId, engineState, gen, audioContext sampleRate/state, track.getSettings), `CAPTURE_STOP` (wallTime, expectedSamples=`wall*rate/1000`, actualSamples, sampleCoverageRatio, pcmDurationMs, pcmDurationOverWall, pcmStats, windowDistribution), `PCM_16K`, `TRANSCRIBE_START/END` (gen, pcm16kSamples/duration, wordCount), `LIFECYCLE_*` (gen, engineState, cancel/dispose/discard)
- `vosk.ts`: `VOSK_INPUT` (samples, sampleRate, duration, RMS/peak), timestamps `acceptStart/End`, `retrieveStart`, `resultReceivedAt`, `inferenceEnd`, `acceptWaveformMs`, `acceptToRetrieveMs`, `inferenceMs`, `transcriptionLength`, `wordCount`, `textPreview` 80 chars local

**Verificação:** sem novos `delay 50/100ms`, sem `300ms` Vosk, sem `AudioWorklet`, sem constraints alteração — conforme §1.

---

## 6. 15 Gravações

**Frases exatas VOZ-008.3 §5 (falar naturalmente, não repetir até "dar certo", registrar cada tentativa):**

- **CURTA ×5:** `"olá"`
- **MÉDIA ×5:** `"posso comer leites vegetais?"`
- **LONGA ×5:** `"quero trocar dois pães por tapioca e meu peso é setenta quilos"`

**Status:** **0/15 executadas** — sem Android físico, sem `getUserMedia`, sem `AudioContext`, sem `MediaStream` em `vitest` `node` env. Instrumentação aguardando device.

---

## 7. Matriz Completa

| # | Tipo | Wall ms | Rate | Chunks | PCM | PCM ms | Coverage | RMS | Active ms | 16k | Vosk ms | Words | Resultado |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| 1 | curta `olá` | — | — | — | — | — | — | — | — | — | — | — | **PENDING** |
| 2 | curta `olá` | — | — | — | — | — | — | — | — | — | — | — | PENDING |
| 3 | curta `olá` | — | — | — | — | — | — | — | — | — | — | — | PENDING |
| 4 | curta `olá` | — | — | — | — | — | — | — | — | — | — | — | PENDING |
| 5 | curta `olá` | — | — | — | — | — | — | — | — | — | — | — | PENDING |
| 6 | média `posso comer leites vegetais?` | — | — | — | — | — | — | — | — | — | — | — | PENDING |
| 7 | média `posso comer leites vegetais?` | — | — | — | — | — | — | — | — | — | — | — | PENDING |
| 8 | média `posso comer leites vegetais?` | — | — | — | — | — | — | — | — | — | — | — | PENDING |
| 9 | média `posso comer leites vegetais?` | — | — | — | — | — | — | — | — | — | — | — | PENDING |
| 10 | média `posso comer leites vegetais?` | — | — | — | — | — | — | — | — | — | — | — | PENDING |
| 11 | longa `quero trocar dois pães por tapioca e meu peso é setenta quilos` | — | — | — | — | — | — | — | — | — | — | — | PENDING |
| 12 | longa `quero trocar dois pães por tapioca e meu peso é setenta quilos` | — | — | — | — | — | — | — | — | — | — | — | PENDING |
| 13 | longa `quero trocar dois pães por tapioca e meu peso é setenta quilos` | — | — | — | — | — | — | — | — | — | — | — | PENDING |
| 14 | longa `quero trocar dois pães por tapioca e meu peso é setenta quilos` | — | — | — | — | — | — | — | — | — | — | — | PENDING |
| 15 | longa `quero trocar dois pães por tapioca e meu peso é setenta quilos` | — | — | — | — | — | — | — | — | — | — | — | PENDING |

**Nenhum valor estimado.** Separação `GOOD`/`PARTIAL`/`EMPTY`/`ERROR` pendente de logs reais.

---

## 8. WAV

**Controle:** `moonshire.wav` (2.3MB 12s 44.1kHz stereo → 16k mono) → Vosk.

**Status:** **PENDING em Android** — histórico **PASS** em desktop dev/prod (VOZ-004-R4: 122 chars `"aqui você preparar seus roteiros..."`, `samples 192k`, `inference 5182ms dev / 5313ms prod`, `sampleRate 16000`). Com `NEXT_PUBLIC_VOICE_DEBUG=1` deve logar `VOSK_INPUT` 192k, `VOSK_RESULT` 122 chars, `wordCount ~22`, não alterar fixture.

---

## 9. Repetição (Segunda e Terceira Gravação Consecutiva)

**Procedimento VOZ-008.2 §5:** `gravar → transcrever → gravar → transcrever` sem reload (e terceira se necessário) — verificar reuso engine `READY`, `gen` não descartado, `LIFECYCLE_DELIVERED` 2×/3×.

**Status:** **PENDING** — lógica `gen` verificada em `voiceController.test.ts` 15 testes, não em device físico.

---

## 10. Cancel

**Procedimento:** `START` → `CANCEL` durante gravação.

**Esperado:** `LIFECYCLE_CANCEL` `prevGen→newGen`, sem `TRANSCRIBE_START`, tracks liberadas, estado correto, sem callback tardio.

**Status:** **PENDING**.

---

## 11. Permission

**Procedimento:** 1) negar permissão microfone, 2) iniciar gravação, 3) observar, 4) conceder novamente, 5) repetir gravação.

**Esperado:** `failCapture permission_denied`, `engineState ERROR` → após conceder retorna a operacional.

**Status:** **PENDING**.

---

## 12. Unmount

**Procedimento:** iniciar gravação → desmontar/navegar ChatAssistant → retornar → nova gravação.

**Esperado:** `LIFECYCLE_DISPOSE` com `gen` incrementado, listeners limpos, Worker não vaza, resultado anterior descartado (`LIFECYCLE_DISCARD`), nova gravação OK.

**Status:** **PENDING**.

---

## 13. Network

**Verificar durante execução (observar, não alterar):**

- Chunk Vosk `9cb48f3a.9b65b6a08a534e7d.js` 5.79MB — **não precache** (`maximumFileSizeToCache 5MB` VOZ-005 §8) — `HTTP 200` esperado (observado em build)
- Modelo `vosk-model-small-pt-0.3.tar.gz` 32.4MB `HTTP 200` `application/gzip` — Worker fetch same-origin, pode cair em `defaultCache` runtime (VOZ-008 §13)
- Worker inicializado (classic base64 Blob URL)
- **0** POST de áudio, **0** `/api/stt`, **0** Blob upload, **0** Supabase `audio` (grep `voice` + Network `audio/` 0 em VOZ-006)

**Não alterar cache/PWA.**

**Status:** **PENDING** — sem Network tab Android.

---

## 14. Capture

**Comparação VOZ-008.3 §10:** `recordingWallTimeMs` vs `pcmDurationMs`, `expectedSamples` (`wall*rate/1000`) vs `actualSamples` (`pcm.length`), `sampleCoverageRatio`.

**H1 promovida só se discrepância repetível e correlacionada com resultados ruins (ex: curta OK com coverage 0.98, longa falha com coverage 0.75).**

**Status:** **PENDING** — sem wallTime/pcmDuration coletados. Desktop fake indício `12288` vs `16000` (0.768s vs 1s) é **evidência de possível underrun**, não prova Android (VOZ-007 §10).

---

## 15. PCM

**Comparação VOZ-008.3 §11:** `RMS`, `peak`, `activeAudioMs`, `silenceRatio`, `leading/trailingSilenceMs`, `windowDistribution` 100ms.

- Wall 5s, PCM 4.9s, **RMS alto** → captura temporal adequada — downstream é candidato.
- Wall 5s, PCM 4.9s, **RMS≈0** → problema anterior/na captura do sinal.

**Não concluir sem repetição.**

**Status:** **PENDING**.

---

## 16. Resampling

**Verificar:** `recordedRate` (48000 comum Android, 44100 alternativo, 16000 se respeitado) → `pcm16k.length` → `pcm16k.durationMs`, `durationRatio` (VOZ-008 `RESAMPLING`).

**Se consistente:** H3 perde força. Se discrepância sistemática (ex: `inputDuration 3000ms` → `outputDuration 200ms`): H3 candidata.

**Não modificar resampling.**

**Status:** **PENDING**.

---

## 17. Vosk

**Verificar:** `VOSK_INPUT` samples/rate/duration/RMS/peak, `acceptWaveformMs`, `accept→retrieve ms` (esperado ~100ms, medido `acceptEnd→retrieveStart`), `inferenceMs`, `VOSK_RESULT` `transcriptionLength`/`wordCount`.

**Se PCM correto + sinal fala correto + samples Vosk corretos em 16k MAS resultado parcial:** classificar **H2** como principal hipótese (VOZ-008.3 §12).

**Não corrigir delay nesta sprint.**

**Status:** **PENDING**.

---

## 18. Lifecycle

**Se Vosk produz texto coerente MAS `onTranscript` não chega ao `ChatAssistant.input`:**

- Verificar `gen` antes/depois, `engineState`, `LIFECYCLE_DISCARD` vs `DELIVERED`, `controller`/`hook`/`setInput` (`useVoiceInput.ts:38` `setTranscript` + `options.onTranscript`, `ChatAssistant.tsx:517` `state.setInput`).

**Cenário H5:** `VOSK_RESULT` correto → `LIFECYCLE_DISCARD` (gen mudou por `cancel`/`dispose`/`unmount`) → input não atualizado.

**Status:** **PENDING**.

---

## 19. H1 — CAPTURE

**Status:** **NÃO CONCLUSIVA**

- **Evidência a favor:** nenhuma coletada em Android (PENDING). Indício histórico desktop fake não prova.
- **Evidência contra:** nenhuma.
- **Métricas pendentes:** `sampleCoverageRatio`, `pcmDurationMs`, `chunks.length`.
- **Testes relevantes:** §7 matriz 15× — PENDING.

---

## 20. H2 — VOSK TIMING

**Status:** **NÃO CONCLUSIVA** — requer `pcmDuration≈wallTime` + `RMS` alto + `VOSK samples` 16k corretos **MAS** `wordCount` parcial repetível. Pendente `accept→retrieve`.

---

## 21. H3 — RESAMPLING

**Status:** **NÃO CONCLUSIVA** — requer `recordedRate`→`pcm16k` duração inconsistente sistemática. Pendente `durationRatio`.

---

## 22. H4 — WORKER/WASM

**Status:** **NÃO CONCLUSIVA** — requer PCM correto + resampling correto + Vosk input correto + runtime anômalo (`VOSK_ERROR`, `inferenceMs` anômalo, Worker não inicializado). Pendente `VOSK_ERROR` e Network.

---

## 23. H5 — LIFECYCLE / UI

**Status:** **NÃO CONCLUSIVA** — requer Vosk correto mas Chat input não recebe (gen discard). Pendente `LIFECYCLE_DELIVERED` vs `DISCARD`.

---

## 24. Primeiro Estágio Incorreto

```
MICROFONE         ? (PENDING — sem device)
  ↓
CAPTURE           ? (PENDING — wallTime vs pcmDuration)
  ↓
PCM               ? (PENDING — RMS/activeAudio)
  ↓
RESAMPLING        ? (PENDING — durationRatio)
  ↓
PCM 16 kHz        ? (PENDING)
  ↓
VOSK acceptWaveform ? (PENDING — samples/rate)
  ↓
retrieve/final    ? (PENDING — accept→retrieve)
  ↓
resultado         ? (PENDING — wordCount)

ÚLTIMO ESTÁGIO CORRETO:  WAV histórico (moonshire.wav 12s → 122 chars) prova Vosk funciona offline (VOZ-004-R4) — não prova mic real.
PRIMEIRO ESTÁGIO INCORRETO: DESCONHECIDO até matriz §7 coletada.

Não usar probabilidade como prova (VOZ-008.3 §17). Conclusão deve nascer de dados: "15/15 gravações apresentaram coverage X–Y e parciais apresentaram..., portanto H1...".
```

**Objetivo VOZ-008.2 §17 não atingido por bloqueio.**

---

## 25. Último Estágio Correto

**WAV histórico** (VOZ-004-R4) — último estágio comprovadamente correto **fora** do pipeline mic. Para pipeline mic, **nenhum estágio confirmado correto em Android** sem coleta.

---

## 26. Causa Raiz

```
ROOT CAUSE NOT YET CONFIRMED
```

**Critério VOZ-008.3 §17:** só confirmar com métrica + repetível + correlação + estágio localizado + plausibilidade. **Sem matriz §7, nenhum critério preenchido.**

---

## 27. Nível de Confiança

**Baixo — BLOQUEADO por falta de device, não por falha de instrumentação.**

- **Instrumentação:** alta confiança (VOZ-008 validada: `tsc 0`, `vitest 17/258`, `build 28`, flag guardada, thresholds `0.01`/`100ms` documentados)
- **Hipóteses:** confiança **não avaliada** (sem dados)
- **Próximo passo:** alta confiança no plano (medir antes de modificar)

---

## 28. Recomendação VOZ-009

**Não criar VOZ-009.** Próxima sprint é **VOZ-008.4** (nomenclatura VOZ-008.1 §20, VOZ-008.2 §21, VOZ-008.3 §21).

**VOZ-008.4 — Coleta Complementar Direcionada (mesma instrumentação, sem correção)** — mas conforme **VOZ-008.3 §22 regra de encerramento**, se a única pendência continuar sendo ausência de dispositivo, **não continuar criando sprints de instrumentação**.

**A partir deste ponto (3× BLOCKED: VOZ-008.1, VOZ-008.2, VOZ-008.3), o projeto deve permanecer:**

```
ROOT CAUSE NOT YET CONFIRMED
```

**até existir execução Android real.** Não inventar alternativa equivalente, não simular, não corrigir por hipótese.

**Para desbloquear:**

1. Provisionar **Android 15 físico + Chrome + `NEXT_PUBLIC_VOICE_DEBUG=1` + `npm run build && npm run start` HTTPS** (Vercel env var, rebuild) — ou `BrowserStack` Android 15 real como fallback documentado (VOZ-008.2 §15)
2. Executar **§5–6 deste relatório** (15 gravações frases exatas + WAV + 2ª/3ª gravação + cancel + permission denied + unmount + Network) com `chrome://inspect` remote debugging, preservar logs `[VOICE_DEBUG]` completos com timestamps
3. Preencher matriz §7 e métricas §6 (coverage, RMS, activeAudio, duration, accept→retrieve, wordCount) — sem estimados, PENDING se não executado
4. Classificar H1–H5 `CONFIRMADA/ELIMINADA/NÃO CONCLUSIVA` com evidência a favor/contra + métricas

**Somente após `ROOT CAUSE IDENTIFIED — H1/H2/H3/H4/H5` em VOZ-008.4, criar `VOZ-009 — Correção Específica` (uma correção mínima: H1→capture, H2→Vosk timing/finalização, H3→resampling, H4→Worker/WASM/runtime, H5→lifecycle/controller/UI).**

**Se VOZ-008.4 ainda BLOCKED (sem device):** não criar `VOZ-008.5`; escalar provisionamento e manter `ROOT CAUSE NOT YET CONFIRMED`.

---

## Gates

| Gate | Esperado (VOZ-008.3 §21) | Observado | Status |
|---|---|---|---|
| Android físico | utilizado | **0 devices** (`adb: not found`, `emulator.exe: False`) | **FAIL — BLOCKED** |
| Chrome | utilizado | — | **FAIL** |
| HTTPS | — | — | **FAIL** |
| debug ativo | `NEXT_PUBLIC_VOICE_DEBUG=1` | `undefined` neste CI (correto quando flag 0) | **PARTIAL — pronta, não ativa** |
| logs coletados | `[VOICE_DEBUG]` | **0** | **FAIL** |
| curta ×5 | 5 reais | 0 | **FAIL** |
| média ×5 | 5 | 0 | **FAIL** |
| longa ×5 | 5 | 0 | **FAIL** |
| 15/15 reais | 15 | 0 | **FAIL** |
| WAV | 1 | histórico PASS, não re-executado com flag | **PARTIAL** |
| segunda transcrição | 1 | lógica `gen` testada, não físico | **PARTIAL** |
| cancel | 1 | — | **FAIL** |
| permission denied | 1 | — | **FAIL** |
| unmount | 1 | — | **FAIL** |
| Network | observado | `chunk 5.79MB`/`tar.gz 32MB` não observado em device | **FAIL** |
| 0 áudio enviado | 0 | **PASS** (instrumentação sem envio) | **PASS** |
| 0 áudio persistido | 0 | **PASS** | **PASS** |
| tsc | PASS 0 | **PASS 0** | **PASS** |
| vitest | PASS 17/258 | **PASS 17/258** | **PASS** |
| build | PASS 28 | **PASS 28** | **PASS** |
| relatório | `docs/VOZ-008.3...` | **PASS** (este) | **PASS** |

**Gates técnicos CI PASS; gates físicos Android FAIL por falta de device — não por regressão. Terceira sprint consecutiva BLOCKED pela mesma causa.**

---

*VOZ-008.3 pertence à VOZ-008. Não avançar para VOZ-009. Próxima sprint: **VOZ-008.4 apenas se device físico provisionado; caso contrário, manter `ROOT CAUSE NOT YET CONFIRMED` até execução Android real.***
