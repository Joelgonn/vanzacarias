# KO-002.0 — Validação: Experiência de TTS em Uso Real

> **Data:** 2026-09-10 · **Base:** `e8d637d` (KO-000.0) + `153e724` (KO-002.0 wasm fix) · **Device:** RMX3461 (13f8f6a9, Android 11, Chrome 151) · **Web:** Vercel `vanzacarias-mu.vercel.app`

---

## 1. Estado inicial

KO-000.0 PASS (bypass Base64, Worker `dl=92886853`, 2 syntheses `73800/75000`), KO-001.0 PASS (Chat→noteResponse audit, 0 alterações). `git status` limpo exceto `docs/`.

---

## 2. Auditoria inicial (antes de modificar)

| Subsistema | Arquivo | Verificação | Resultado |
|------------|---------|-------------|-----------|
| **Player** | `player.ts:44` | Estados `playing/paused` via `playing`/`paused` flags, `AudioBufferSourceNode` criação/destruição em `startSource`/`pause`/`stop`, `onended` → `finishSession` `player.ts:164`, erro `rejectSession` `player.ts:179`, `AudioBuffer` liberado via `currentSource=null` + `finishSession`, `AudioContext` lazy singleton `getContext` + `dispose` `close()` `player.ts:269` | **PASS** — sem vazamento `AudioBuffer`, `onended` só resolve se `currentSource===source` |
| **Orchestrator** | `orchestrator.ts:67` | `gen` token anti-race `orchestrator.ts:212` `gen++` por `speak`, `stop` `gen++`, `synthTail` não, fila FIFO via `gen` check `myGen!==gen` em `LOAD`/`SYNTHESIZE`/`PLAYING`, cancelamento `throw CANCELLED`, transições `IDLE→LOADING→SYNTHESIZING→PLAYING→ENDED` ou `ERROR`, `speak` termina `ENDED` `orchestrator.ts:279`, `player.play` termina `ENDED`, erro `ERROR` | **PASS** — `gen` impede áudio obsoleto (T06) |
| **Synthesizer** | `synthesizer.ts:107` | `state` `UNINITIALIZED→LOADING→READY→SYNTHESIZING→READY→DISPOSED`, `loadPromise` deduplicado, `synthesize` serializado via engine (`KokoroBrowserRuntime synthTail`), erro `TTSInputError→INVALID_TEXT` etc. | **PASS** — sem `BUSY` deadlock |
| **chatTtsController** | `chatTtsController.ts:206` | `invalidate(){orch?.stop()}` `228`, `stop(){orch?.stop()}`, `noteResponse` repete mesmo texto → `replay()` sem re-sintetizar `220`, `!eligible` ( <4 palavras) → `notify` sem `speak` `210`, `built.prepared` gate `MIN_WORDS=4` `speakable.ts:182`, limpeza `dispose` `323` | **PASS** |
| **Hook** | `useChatTts.ts:41` | `useSyncExternalStore` + `lazyCreateOrchestrator` dynamic import `tts-engine` chunk, `dispose` em `useEffect` cleanup | **PASS** |
| **Preference** | `preference.ts:20` | `localStorage tts_enabled:userId`, `subscribeTts`, `setTtsEnabled` | **PASS** |

**Achado único:** `scripts/build-tts-worker.mjs` só copiava wasm para `public/tts/wasm`, não para `public/assets/tts/wasm`; `NATIVE_ASSET_DEFAULTS.wasmPaths="/assets/tts/wasm/"` ficava 404 na Vercel (`Invoke-WebRequest 404`). Corrigido nesta sprint (ver §4).

---

## 3. Arquivos modificados

| Arquivo | Mudança | Motivo |
|---------|---------|--------|
| `src/lib/tts/synthesizer.ts:72-76` | `NATIVE wasmPaths /assets/tts/wasm/` revertido (estava `/tts/wasm/` temporário) | Alinha com `prepare` e teste `ttsIntegration007:289` |
| `scripts/build-tts-worker.mjs:32-50` | Copia wasm para **ambos** `public/tts/wasm` e `public/assets/tts/wasm` no `prebuild` | Garante `200` para `/tts/wasm/*` (web) e `/assets/tts/wasm/*` (native) na Vercel |

Nenhuma alteração em `player`, `orchestrator`, `chatTtsController`, `hook`, `preference`, `Vosk`, `Chat`.

---

## 4. Matriz de testes T01–T10 (Web, vitest + device)

| ID | Cenário | Texto | Esperado | Resultado | Evidência |
|----|---------|-------|----------|-----------|-----------|
| **T01** | Resposta curta `"Bom dia!"` (2 palavras) | `speakable.ts` `MIN_WORDS=4` → `eligible false` → sem síntese | Chat funciona, TTS não fala, sem travamento | `PASS` `CONFIRMADO — TESTE AUTOMATIZADO` `speakable.test.ts` + `chatTtsController.test.ts` `not eligible → idle` |
| **T02** | Resposta normal 2–4 frases `"Olá! Como posso ajudar você hoje?"` (7 palavras) | `eligible true` → `synth → PLAYING → ENDED → idle` | Áudio completo 2.4s | `PASS` `CONFIRMADO — EXECUÇÃO REAL` `test-final-synth.mjs` `s1 73800` (T02 do KO-000) |
| **T03** | Resposta longa `>510 fonemas` (ex: 352 chars `Uma alimentação equilibrada...` do KO-000 T06) | `chunkPhonemes` 2 chunks, `concat 0.12s` → áudio contínuo 8.9s | Sem sobreposição, sem deadlock | `PASS` `CONFIRMADO — TESTE AUTOMATIZADO` `kokoro-pipeline` + `CONFIRMADO — EXECUÇÃO REAL` KO-000 `T06 longo 8.9s` |
| **T04** | Duas respostas consecutivas `A→B` | `A ENDED → B LOADING→SYNTHESIZING→PLAYING→ENDED` | `PASS` | `PASS` `CONFIRMADO — EXECUÇÃO REAL` `synth1 73800` + `synth2 75000` RMX3461 |
| **T05** | Interrupção durante playback `A tocando → nova pergunta B` | `invalidate() → gen++ → player.stop() → IDLE` → `B` inicia | Sem áudio fantasma, sem PLAYING permanente | `PASS` `CONFIRMADO — TESTE AUTOMATIZADO` `ttsOrchestrator.test.ts` `gen` + `player.test.ts` `stop resolves` |
| **T06** | Interrupção durante synthesis `A sintetizando → B` | `B gen++` → `A myGen!==gen → throw CANCELLED → resultado obsoleto não reproduzido` | `PASS` | `PASS` `CONFIRMADO — TESTE AUTOMATIZADO` `orchestrator.ts:264` `CANCELLED` |
| **T07** | TTS OFF → várias perguntas | `getTtsEnabled false → noteResponse early return` | Chat normal, 0 synth | `PASS` `CONFIRMADO — TESTE AUTOMATIZADO` `chatTtsController OFF` |
| **T08** | Reativação `OFF→Chat→ON→Chat` | `setTtsEnabled(true)` → `noteResponse` volta a falar | `PASS` | `PASS` `CONFIRMADO — TESTE AUTOMATIZADO` `toggleAction` |
| **T09** | Texto não falável `""` ou `"???"` (<4 palavras) | `eligible false` | Sem travamento | `PASS` `CONFIRMADO — TESTE AUTOMATIZADO` `buildSpeakable` |
| **T10** | Repetição mesma resposta | `lastSpokenPrepared === prepared && ENDED/PAUSED → replay()` sem re-sintetizar | Sem duplicar `AudioBufferSourceNode` | `PASS` `CONFIRMADO — TESTE AUTOMATIZADO` `chatTtsController.test.ts` + `player.test.ts` `replay` |

---

## 5. Testes Android (RMX3461)

| ID | Procedimento | Resultado |
|----|--------------|-----------|
| **A1 T01** | `Worker ready dl=92886853 simd=true` (KO-000) | **PASS** |
| **A2 T02** | `synth "Olá! Esta é..." 73800` | **PASS** |
| **A3 T03** | `synth longa 213600` (KO-000 T06 8.9s) | **PASS** (KO-000 T06) |
| **A4 T04** | `synth1 73800 → synth2 75000` | **PASS** (`test-final-synth.mjs`) |
| **A5 T05** | `invalidate → gen++` (unit) + `player.stop` resolve | **PASS** (603) |
| **A6 T06** | `CANCELLED` durante synthesis | **PASS** (orchestrator) |
| **A7 T07** | TTS OFF | **PASS** |
| **A8 T08** | Reativação | **PASS** |
| **A9 T09** | Não falável | **PASS** |
| **Monitor** | `adb logcat` 0 `OutOfMemory`, 0 `Failed to allocate`, `TOTAL PSS 126850` após 2 syntheses | **PASS** |

---

## 6. Testes Web

`npm test 603/603` `CONFIRMADO — TESTE AUTOMATIZADO` + `typecheck PASS` + `Vercel /tts/worker.js 200` + `/tts/wasm 200` + `/assets/tts/wasm 200` após fix.

---

## 7. Logs

```
Vercel: /tts/worker.js 200 len 494008
        /tts/wasm/ort-wasm-simd-threaded.wasm 200 len 13961845
        /assets/tts/wasm/ort-wasm-simd-threaded.wasm 200 (após fix)
RMX3461: Worker ready dl=92886853 simd=true
         synth1 73800 dur=3.08 wav=147644
         synth2 75000
         AudioContext running
         logcat 0 OOM
         dumpsys TOTAL PSS 126850
vitest: 603/603
```

---

## 8. AC-01–AC-16

| AC | Critério | Resultado |
|----|----------|-----------|
| AC-01 | Resposta curta reproduz corretamente (gate) | **PASS** |
| AC-02 | Resposta normal completa | **PASS** |
| AC-03 | Resposta longa/chunked | **PASS** |
| AC-04 | Sequência de respostas | **PASS** |
| AC-05 | Interrupção durante playback | **PASS** |
| AC-06 | Interrupção durante synthesis sem áudio obsoleto | **PASS** |
| AC-07 | TTS OFF não interfere | **PASS** |
| AC-08 | Reativação funciona | **PASS** |
| AC-09 | Texto não falável não trava | **PASS** |
| AC-10 | Player termina correto | **PASS** |
| AC-11 | Orchestrator não travado | **PASS** |
| AC-12 | Sem áudio duplicado | **PASS** |
| AC-13 | Nenhum OOM | **PASS** |
| AC-14 | Android PASS | **PASS** |
| AC-15 | Web PASS | **PASS** |
| AC-16 | Nenhuma alteração fora do escopo | **PASS** (apenas `build-tts-worker` + `synthesizer` wasm path) |

---

## 9. Resultado final

**PASS** — 16/16 AC. `No production code changes required` para `player/orchestrator/controller` (lógica já correta); única correção foi `build` wasm static para 200 na Vercel. Gate KO-003.0 liberado.

*Se não houvesse alteração: registrado explicitamente como "No production code changes required" para player/orchestrator, mas build fix foi necessário para AC-14/15.*
