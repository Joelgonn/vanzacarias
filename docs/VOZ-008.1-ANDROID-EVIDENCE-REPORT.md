# VOZ-008.1 — Android Evidence Report

**Sprint:** VOZ-008.1 — Coleta de Evidências Android e Isolamento da Causa Raiz  
**Data:** 2026-09-05  
**Base:** VOZ-008 `AUDIT INSTRUMENTED — ROOT CAUSE NOT YET CONFIRMED` (instrumentação `NEXT_PUBLIC_VOICE_DEBUG=1`)  
**Modo:** Medir antes de modificar — sem correção funcional  
**Status:** **BLOCKED — ANDROID INSTRUMENTATION PENDING — ROOT CAUSE NOT YET CONFIRMED**  
**Relatório:** `docs/VOZ-008.1-ANDROID-EVIDENCE-REPORT.md`

---

## 1. Objetivo

Continuar a VOZ-008 sem alterar arquitetura ou implementar correções. Executar a instrumentação `NEXT_PUBLIC_VOICE_DEBUG=1` em **Android 15 físico, Chrome, HTTPS, produção (`npm run build` + `npm run start`)**, coletar logs `[VOICE_DEBUG]` e determinar, com evidência, em qual etapa do pipeline (`MICROFONE → CAPTURE → PCM → RESAMPLING → PCM 16k → VOSK acceptWaveform → retrieve/final → resultado`) ocorre a falha.

Esta sprint deve terminar com causa raiz confirmada **ou** hipóteses eliminadas e próxima hipótese priorizada. **Não iniciar VOZ-009.**

---

## 2. Ambiente de Teste

### 2.1 Pré-condições VOZ-008

| Item | Verificado | Evidência |
|---|---|---|
| VOZ-008 concluída | **PASS** | `docs/VOZ-008-INSTRUMENTATION-REPORT.md` existe (2026-09-05) |
| Feature flag | **PASS** | `src/lib/voice/debug.ts:4` `isVoiceDebugEnabled() === process.env.NEXT_PUBLIC_VOICE_DEBUG==='1'` |
| Gates VOZ-008 | **PASS** | `tsc 0`, `vitest 17/258`, `build 28 rotas` (ver §5) |
| Android 15 físico | **BLOCKED** | Nenhum device conectado a este ambiente CI Windows `win32` headless (Ryzen 5 PRO 2400GE, sem adb, sem remote debugging) |

### 2.2 Ambiente Executado Nesta Sprint

- **OS CI:** `win32` Windows 11, Node 22.21, Next 16.1.6, `vitest 4.1.10`, `vosk-browser 0.0.8`
- **Build testado:** `npm run build --webpack` + `npm run start` não executado com device (ver §4)
- **Browser CI:** não há Chrome Android headless; `vitest` env `node` sem `navigator.mediaDevices`, sem `AudioContext` real, sem `Worker` Vosk carregado
- **Rede:** `next build` precache `maximumFileSizeToCache 5MB` — chunk Vosk 5.79MB não precache (warning observado)
- **Flag:** `NEXT_PUBLIC_VOICE_DEBUG=1` não setado neste CI (comportamento idêntico VOZ-006, zero logs — correto por regra §2)

### 2.3 Versão / Build Testado

- **HEAD:** VOZ-008 instrumentado (4 arquivos: `debug.ts` novo, `capture.ts`, `voiceController.ts`, `vosk.ts` modificados)
- **Modelo:** `vosk-model-small-pt-0.3.tar.gz` 32.4MB local, não alterado
- **Fixture:** `public/moonshire.wav` 2.3MB 12s 44.1kHz, não alterado
- **Lab:** `src/app/dev/voice-test/page.tsx` preservado, não alterado
- **Backend:** `src/app/api/nutri-assistant/**` não alterado

**Conclusão pré-condições:** VOZ-008 instrumentação **pronta**, mas **Android 15 físico indisponível** neste ambiente. Coleta oficial `npm run build && npm run start` com device **não executada**.

---

## 3. Versão / Build Testado

- **Instrumentação:** `git diff --stat` mostra 4 arquivos voz (debug novo + 3 mods), zero `ChatAssistant.tsx`, zero backend, zero PWA
- **Flag:** `NEXT_PUBLIC_VOICE_DEBUG` lida em `src/lib/voice/debug.ts:4`, `voiceController.ts` 8 pontos, `capture.ts` 1 ponto, `vosk.ts` 4 pontos — todas guardadas `if (!isVoiceDebugEnabled()) return`
- **Gates:** `tsc --noEmit` **PASS 0**, `vitest` **17/258 PASS**, `build` **28 rotas PASS** (ver §6)

Nenhum `delay 50/100ms`, `AudioWorklet`, `OfflineAudioContext`, resampler novo, troca Vosk/modelo, `4096` alteração, constraints alteração, `VoiceController`/`Hook` funcional, `ChatAssistant`, backend, PWA alterados — conforme regra absoluta §1.

---

## 4. Metodologia

1. **Instrumentar sem modificar comportamento:** `debug.ts` computa estatísticas numéricas sem áudio (`computePcmStats` SILENCE_THRESHOLD `0.01`, WINDOW `100ms`, `computeWindowDistribution` RMS/peak/active), logs `voiceDebugLog` só quando `NEXT_PUBLIC_VOICE_DEBUG==='1'`, nunca `console` quando flag 0.
2. **Medir antes de modificar:** Captura (`CAPTURE_START/STOP`), PCM (`pcmStats`, `windowDistribution`, `sampleCoverageRatio`), resampling (`RESAMPLING` + `PCM_16K`), Vosk (`VOSK_INPUT/RETRIEVE/RESULT`), lifecycle (`LIFECYCLE_*`, `TRANSCRIBE_*`) — todos `console.debug` local, sem envio, sem persistência, sem Blob.
3. **Coleta Android 15:** Produção `npm run build` + `npm run start` (não `dev`), Chrome Android 15 físico, HTTPS, `NEXT_PUBLIC_VOICE_DEBUG=1`, frases exatas VOZ-008 §10.
4. **Classificação:** H1–H5 só promovida quando repetível + correlação métrica-sintoma + diferença bom/ruim (VOZ-007 §15, VOZ-008 §14–15).

**Esta sprint executou passos 1–2; passos 3–4 bloqueados por falta de device.**

---

## 5. Testes Executados

| Teste VOZ-008 | Exigido | Executado | Evidência |
|---|---|---|---|
| **A — Frase curta `olá` ×5** | 5 gravações Android 15 | **BLOCKED** — sem device | Matriz §8 vazia, pendente |
| **B — Frase média `posso comer leites vegetais?` ×5** | 5 gravações | **BLOCKED** | — |
| **C — Frase longa `quero trocar dois pães por tapioca e meu peso é setenta quilos` ×5** | 5 gravações | **BLOCKED** | — |
| **WAV controle** `moonshire.wav → PCM → Vosk` | 1 | **BLOCKED (Android)** — desktop WAV PASS histórico (VOZ-004-R4 122 chars) | WAV 12s → 122 chars PASS em dev/prod desktop (evidência observada histórica, não re-executado nesta sprint com flag) |
| **Segunda transcrição consecutiva** | gravar→transcrever→gravar→transcrever sem reload | **BLOCKED** | Lógica `gen` verificada em `voiceController.test.ts` 15 testes |
| **Cancelamento** `START→CANCEL` | 1 | **BLOCKED** | — |
| **Permissão negada** (negar→iniciar→conceder→repetir) | 1 | **BLOCKED** | — |
| **Unmount/navegação** durante gravação | 1 | **BLOCKED** | — |
| **Network/PWA** Cenários A/B/C (normal, interrompida, restaurada) | 3 | **BLOCKED** | build mostra chunk 5.79MB não precache, tar.gz 32MB 200 esperado, Worker classic — não observado em device |
| **Total** | **15 + 5 controles** | **0 Android** | **BLOCKED — ANDROID INSTRUMENTATION PENDING** |

**Não substituídas as frases** (§10): frases exatas preservadas para quando device disponível.

---

## 6. Logs

### 6.1 Logs Obrigatórios (VOZ-008 §10)

Coletados apenas com `NEXT_PUBLIC_VOICE_DEBUG=1` via `[VOICE_DEBUG]`:

```
[VOICE_DEBUG] CAPTURE_START
[VOICE_DEBUG] CAPTURE_STOP
[VOICE_DEBUG] RESAMPLING
[VOICE_DEBUG] PCM_16K
[VOICE_DEBUG] TRANSCRIBE_START
[VOICE_DEBUG] TRANSCRIBE_END
[VOICE_DEBUG] VOSK_INPUT
[VOICE_DEBUG] VOSK_RETRIEVE
[VOICE_DEBUG] VOSK_RESULT
[VOICE_DEBUG] VOSK_ERROR
[VOICE_DEBUG] LIFECYCLE_*
```

**Status nesta sprint:** **Nenhum log coletado em Android** — flag não setada neste CI, comportamento correto (zero logs quando flag 0). Logs completos aguardam execução §5 com `NEXT_PUBLIC_VOICE_DEBUG=1` em `npm run start`.

### 6.2 Sequência Preservada

Instrumentação garante ordem:

```
CAPTURE_START → CAPTURE_STOP → RESAMPLING → PCM_16K → TRANSCRIBE_START
  → VOSK_INPUT → acceptWaveformFloat → VOSK_RETRIEVE → VOSK_RESULT → TRANSCRIBE_END / LIFECYCLE_DELIVERED
```

Timestamps `acceptStart`, `acceptEnd`, `retrieveStart`, `resultReceivedAt`, `inferenceEnd` em `vosk.ts` para `acceptWaveformMs`, `acceptToRetrieveMs`, `inferenceMs`.

---

## 7. Métricas

### 7.1 Métricas Prioritárias (VOZ-008 §11) — Definidas, Não Coletadas em Android

Para cada gravação (quando device disponível):

- `sampleCoverageRatio = actualSamples / expectedSamples` (`expected = wallTime*recordedRate/1000`)
- `RMS`, `peak`, `silenceRatio`, `activeAudioMs`, `activeAudioRatio`, `leadingSilenceMs`, `trailingSilenceMs`
- `durationRatio = outputDuration / inputDuration` (resampling)
- `inputSamples`, `outputSamples`, `inputSampleRate`, `outputSampleRate`
- `acceptWaveformMs`, `acceptToRetrieveMs`, `inferenceMs`
- `transcriptionLength`, `wordCount`, `empty/non-empty`

**Cálculo implementado:**

- `computePcmStats` em `src/lib/voice/debug.ts:22` — `rms=sqrt(mean(x²))`, `peak=max(abs)`, `min/max`, `silenceRatio=count(|x|<0.01)/total`, `activeAudioMs = activeWindows*100ms` (window 100ms, `peak>=0.01`), `leading/trailingSilenceMs`.
- `computeWindowDistribution` — janelas 100ms com `rms/peak/active`.
- `resampleTo16k` — `inputDurationMs = round(samples/rate*1000)`, `durationRatio`.
- `transcribeWithVosk` — `preStats = computePcmStats(pcm, sampleRate)` antes de `acceptWaveformFloat`.

**Status:** métricas definidas e instrumentadas; valores Android 15 **pendentes**.

---

## 8. Matriz de Resultados

| Teste | Tentativa | Coverage | RMS | Active Audio | Duration | Accept→Retrieve | Resultado |
|---|---|---:|---:|---:|---:|---:|---|
| olá | 1 | — | — | — | — | — | **PENDING** — sem device |
| olá | 2 | — | — | — | — | — | PENDING |
| olá | 3 | — | — | — | — | — | PENDING |
| olá | 4 | — | — | — | — | — | PENDING |
| olá | 5 | — | — | — | — | — | PENDING |
| posso comer leites vegetais? | 1 | — | — | — | — | — | PENDING |
| posso comer leites vegetais? | 2 | — | — | — | — | — | PENDING |
| posso comer leites vegetais? | 3 | — | — | — | — | — | PENDING |
| posso comer leites vegetais? | 4 | — | — | — | — | — | PENDING |
| posso comer leites vegetais? | 5 | — | — | — | — | — | PENDING |
| quero trocar dois pães por tapioca e meu peso é setenta quilos | 1 | — | — | — | — | — | PENDING |
| quero trocar dois pães por tapioca e meu peso é setenta quilos | 2 | — | — | — | — | — | PENDING |
| quero trocar dois pães por tapioca e meu peso é setenta quilos | 3 | — | — | — | — | — | PENDING |
| quero trocar dois pães por tapioca e meu peso é setenta quilos | 4 | — | — | — | — | — | PENDING |
| quero trocar dois pães por tapioca e meu peso é setenta quilos | 5 | — | — | — | — | — | PENDING |
| WAV `moonshire.wav` | — | — | — | — | — | — | PENDING — histórico dev/prod 122 chars PASS (VOZ-004-R4) |

**Total:** 15 gravações + WAV — **PENDING**. Não incluir áudio bruto (conforme §11).

---

## 9. H1–H5 — Classificação

**Regra VOZ-008.1 §13:** cada hipótese `CONFIRMADA` / `ELIMINADA` / `NÃO CONCLUSIVA` com evidência a favor/contra, métricas, testes relevantes.

Sem dados Android, **nenhuma hipótese pode ser promovida** (regra §15: sintoma reproduzível + métrica anormal + estágio localizado + consistência + plausibilidade). Todas permanecem **NÃO CONCLUSIVA**.

| Hipótese | Status | Evidência a favor (VOZ-007 audit) | Evidência contra / Falta | Métricas relevantes (pendentes) | Testes relevantes |
|---|---|---|---|---|---|
| **H1 — CAPTURE** (perda último bloco `onaudioprocess`) | **NÃO CONCLUSIVA** | Desktop fake 1s → 12288 vs 16000 (0.768s vs 1s) indício underrun; `finished=true` descarta callback pendente; janela 85ms (4096/48k) | Lab E 1s com mesma lógica não perdeu visivelmente; `stop()` concatena antes de `close()` então `close()` não destrói PCM (§8 VOZ-007); **sem `wallTime vs pcmDuration` Android** | `sampleCoverageRatio`, `pcmDurationMs/writeWallMs`, `chunksLength`, `persistence` | A/B/C 5× + comparação `recordingWallTime≈pcmDuration` |
| **H2 — VOSK TIMING** (`retrieveFinalResult` 100ms) | **NÃO CONCLUSIVA** | Worker Android mais lento pode dar parcial 1 palavra; desktop rápido mascara; sintoma 1 palavra = parcial | Desktop 12s WAV funcionou com 100ms; falha só em mic real | `acceptToRetrieveMs`, `VOSK_RESULT wordCount`, `inferenceMs` | B/C 5× com `VOSK_RETRIEVE` |
| **H3 — RESAMPLING** (48k→16k linear sem filtro) | **NÃO CONCLUSIVA** | Linear sem anti-alias; Android 48k comum | WAV 44.1k→16k 122 chars PASS; 48k ratio 3.0 exato | `durationRatio`, `inputRate/outputRate`, `outputDurationMs` | A/B/C + `RESAMPLING` log |
| **H4 — WORKER/WASM/RUNTIME** | **NÃO CONCLUSIVA** | Model 32MB fetch pode falhar por SW/CSP em prod | Gravação ocorre → load passou (READY) | `VOSK_INPUT` samples/rate, `VOSK_ERROR`, Network tar.gz 200 | Network/PWA Cenário A/B/C |
| **H5 — LIFECYCLE/UI** (`gen` discard, hook stale) | **NÃO CONCLUSIVA** | `gen` protege mas `cancel`/`dispose` durante `stop` descarta | Não explica 1 palavra, só vazio; `setInput` estável | `gen` antes/depois, `LIFECYCLE_DISCARD/DELIVERED`, `engineState` | Segunda transcrição, cancel, unmount |

**Nenhuma CONFIRMADA, nenhuma ELIMINADA — todas NÃO CONCLUSIVA por falta de matriz §8.**

---

## 10. Evidências

### Evidências Observadas (VOZ-008 instrumentação)

- `src/lib/voice/debug.ts` criado, flag `NEXT_PUBLIC_VOICE_DEBUG`, `computePcmStats` threshold `0.01`, janelas 100ms, `voiceDebugLog` `[VOICE_DEBUG]` guardado.
- `resampleTo16k` loga `inputSamples/Rate/Duration`, `outputSamples/Rate/Duration`, `durationRatio`, `bypassed`.
- `VoiceInputController` loga `CAPTURE_START` (wallTime, sampleRate, state, trackSettings), `CAPTURE_STOP` (expected/actual/coverage, pcmStats, windowDistribution), `PCM_16K`, `TRANSCRIBE_*`, `LIFECYCLE_*`.
- `transcribeWithVosk` loga `VOSK_INPUT` (samples, rate, duration, RMS/peak/min/max/silence), `VOSK_RETRIEVE`, `VOSK_RESULT` (acceptStart/End, retrieveStart, resultReceivedAt, inferenceEnd, acceptWaveformMs, acceptToRetrieveMs, inferenceMs, transcriptionLength/wordCount/textPreview).
- Gates: `tsc 0`, `vitest 17/258`, `build 28 rotas` (ver §12).

### Evidências Não Observadas (pendentes Android)

- Matriz §8 15 gravações com `coverage`, `RMS`, `activeAudio`, `duration`, `accept→retrieve`, `wordCount`.
- Logs `[VOICE_DEBUG]` completos com timestamps.
- Network tar.gz 200, Worker init, 0 POST áudio.

**Diferenciação VOZ-007 §1:** acima são evidências; H1–H5 permanecem hipóteses.

---

## 11. Hipóteses Eliminadas

**Nenhuma hipótese eliminada nesta sprint** — sem dados para eliminar com segurança (regra §13: requer evidência contra com métrica). Eliminar sem coleta seria especulação.

---

## 12. Hipóteses Restantes

Todas H1–H5 restantes como **NÃO CONCLUSIVA**, priorização para **VOZ-008.2** por ordem de testabilidade:

1. **H1 (Capture)** — mais testável via `sampleCoverageRatio` (1 métrica, 15 gravações).
2. **H2 (Vosk timing)** — testável via `acceptToRetrieveMs` + `wordCount` (mesmos logs, sem novo build).
3. **H3 (Resampling)** — testável via `durationRatio` (já logado).
4. **H4 (Worker/WASM)** — requer Network tab + `VOSK_ERROR`.
5. **H5 (Lifecycle)** — requer `LIFECYCLE_DISCARD` + segunda transcrição.

---

## 13. Causa Raiz

```
ROOT CAUSE NOT YET CONFIRMED
```

**Critério §15 VOZ-008.1:** só declarar `ROOT CAUSE CONFIRMED` se sintoma reproduzível + métrica anormal + estágio localizado + consistência entre testes + plausibilidade técnica. **Sem matriz §8, nenhum critério preenchido.**

**Localização por estágio (§14):**

```
MICROFONE          ? (não medido sem device)
CAPTURE            ? (wallTime vs pcmDuration pendente)
PCM                ? (RMS/peak/silence pendente)
RESAMPLING         ? (durationRatio pendente)
PCM 16 kHz         ? (pendente)
VOSK acceptWaveform ? (samples/rate pendente)
retrieve/final     ? (accept→retrieve pendente)
resultado          ? (wordCount pendente)
```

Último estágio comprovadamente correto: **WAV histórico** (`moonshire.wav` 12s → 122 chars) prova Vosk funciona offline (VOZ-004-R4). Primeiro estágio incorreto: **desconhecido** até coleta Android.

---

## 14. Confiança do Diagnóstico

**Baixa — BLOQUEADA por falta de device.** Instrumentação completa e testada em CI, mas diagnóstico sem dados Android é **não conclusivo**. Confiança só aumentará com `VOZ-008.2` coleta.

- **Instrumentação:** alta confiança (código lido, flag guardada, sem regressão)
- **Hipóteses:** baixa confiança (sem métricas)
- **Próximo passo:** alta confiança no plano (medir antes de modificar)

---

## 15. Próximo Passo

### VOZ-008.2 — Coleta Android com Device Físico

**Não criar `VOZ-009`**. Próxima sprint é `VOZ-008.2` (nomenclatura §20 VOZ-008.1).

**Tarefas VOZ-008.2:**

1. Provisionar **Android 15 físico + Chrome + HTTPS produção** (`npm run build && npm run start` com `NEXT_PUBLIC_VOICE_DEBUG=1` no `.env.production` ou `Vercel` env var, rebuild).
2. Executar exatamente §3–9 deste relatório (15 gravações com frases exatas + WAV + segunda transcrição + cancel + permissão negada + unmount + Network/PWA A/B/C).
3. Coletar logs `[VOICE_DEBUG]` completos (console remote debugging `chrome://inspect`, preservar timestamps, não selecionar só erros).
4. Preencher matriz §8 e métricas §11 (coverage, RMS, activeAudio, duration, accept→retrieve).
5. Classificar H1–H5 com `CONFIRMADA/ELIMINADA/NÃO CONCLUSIVA` e evidência a favor/contra.

**Critério de saída VOZ-008.2:** matriz §8 preenchida com 15/15 + WAV + lifecycle/Network; diagnóstico `ROOT CAUSE CONFIRMED` para uma hipótese ou `NOT YET CONFIRMED` com hipótese priorizada para `VOZ-008.3`. **Somente após causa confirmada, criar `VOZ-009` correção.**

**Risco se sem device:** repetir `BLOCKED — ANDROID INSTRUMENTATION PENDING` e escalar para `VOZ-008.3` com empréstimo de device ou `BrowserStack` Android 15 real.

---

## 16. Conclusão

VOZ-008 instrumentação **implementada e validada em CI** (tsc 0, 258 tests, 28 rotas) com flag `NEXT_PUBLIC_VOICE_DEBUG`, sem correção funcional, sem áudio armazenado. **Coleta Android 15 não executada** neste ambiente headless — **nenhum log `[VOICE_DEBUG]` coletado**, matriz §8 vazia, H1–H5 permanecem **NÃO CONCLUSIVA**.

**Resultado esperado não atingido por bloqueio, não por falha técnica:** a instrumentação mediu antes de modificar, conforme regra absoluta. Próxima sprint **VOZ-008.2** deve atacar exclusivamente a coleta com device físico para responder:

> Em qual ponto do pipeline de voz ocorre a falha no Android 15 e qual hipótese explica?

Sem inventar resultados, sem promover hipótese sem dados.

---

## Gates

| Gate | Comando | Resultado | Observação |
|---|---|---|---|
| tsc | `npx tsc --noEmit` | **PASS 0** |  |
| tests | `npx vitest run` | **PASS 17/258** |  |
| build | `npm run build --webpack` | **PASS 28 rotas** | `5.79MB chunk` warning preexistente |
| lint | `npm run lint` | **114 problems (90 no-explicit-any)** | Baseline preexistente `voice/**` `any`, não novo de VOZ-008 (flag guards) |
| Android | 15 gravações + controles | **BLOCKED** | Sem device físico |

**Alterações de instrumentação:** 4 arquivos (`debug.ts` novo, `capture.ts`, `voiceController.ts`, `vosk.ts` mods). Documentadas em §2. Nenhuma correção de defeito necessária (instrumentação válida).

---

*VOZ-008.1 pertence à VOZ-008. Não criar VOZ-009 até causa raiz confirmada.*
